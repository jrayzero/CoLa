from ..cola.frontend.block import Block,View
from ..cola.frontend.error import ColaError

def clip3(x, y, z):
  if z < x:
    return x
  elif z > y:
    return y
  else:
    return z

def clip1y(x, bit_depth_Y):
  return clip3(0, (1<<bit_depth_Y)-1, x)
  
